# PLAN.md — Job Application Tracker (MVP)

This file is the project's memory across Claude Code sessions. Claude Code has no memory between sessions, so **this file + git history are the source of truth for progress.** Read it at the start of every session; update it as you go.

Full spec: see `job-tracker-mvp-brief.md` in the repo root.

---

## Working protocol (read every session)

1. **Read `job-tracker-mvp-brief.md` and this file first**, then inspect the current state of the code before doing anything.
2. **Do exactly one milestone per session.** Complete only the milestone named in the prompt. Do **not** start the next milestone — stop when the current one is done and the app runs.
3. **Plan before executing.** Produce an execution plan for the milestone and wait for the user's approval before writing code.
4. **Commit after every working sub-step** with a clear message. Small, frequent commits mean an interrupted session loses only the uncommitted tail.
5. **Update this file as you go** — check off completed items and keep the "Current status" section accurate. The next session depends on it.
6. **End each milestone in a runnable, committed state.** The app should launch and the milestone's slice should work before you stop.
7. If you hit a blocker or make a decision worth remembering, note it under "Decisions & notes" below.

---

## Current status

- **M13 — Interview scheduling + calendar export, done and pushed (2026-07-23/24).** Full write-up moved to [PLAN-ARCHIVE.md](PLAN-ARCHIVE.md) (search "M13"); one-line pointer in "Postponed / deferred" below. Session ended on a live guest→signup migration test that surfaced an unresolved, low-confidence signal (migration failed once, succeeded on retry, root cause unconfirmed) — see the archive entry and `HANDOFF.md` before deciding whether to chase it.
- **Multi-select drag-to-move on the Kanban board (2026-07-23), not a numbered milestone.** Bug: dragging one card of an active multi-selection only moved that single card, leaving the rest of the selection behind. `Board.tsx`'s `handleDragEnd` now checks `selectedIds.has(draggedId) && selectedIds.size > 1` and, when true, calls the existing `handleBulkMove(stage)` (same path the SelectionToolbar's "Move to stage" menu already used) instead of the single-card `moveApplicationStage` call — clears the selection afterward, consistent with every other bulk action. Dragging a card that isn't part of any selection is unchanged. **Verified live** via synthetic `PointerEvent` dispatch (`pointerdown`→several `pointermove` steps past dnd-kit's 8px `activationConstraint`→`pointerup`, with real `await sleep()` between steps) since this repo's browser-preview tool can't drive real drag gestures for dnd-kit (same limitation noted for M11/tracker-reorder) — confirmed both the multi-card case (2 selected cards both moved, selection cleared) and the untouched single-card case. Commit `5957b81`.
- **Filter-dropdown clipping-behind-sidebar bug, found and fixed same session (2026-07-23).** Side effect of the Export XLSX reposition below: once "Stage" became the leftmost button in `TableView.tsx`'s toolbar, `MultiSelectFilter`'s dropdown (previously `right-0`, opening leftward) extended past the content area's left edge on narrow layouts, straight into the sidebar's screen region — the `overflow-hidden` main-content wrapper in `Board.tsx` clipped that portion, which just revealed the sidebar underneath (not a z-index/stacking bug, confirmed by walking the actual computed-style ancestor chain via `javascript_tool` before touching anything). Fixed by changing `MultiSelectFilter.tsx`'s dropdown alignment to `left-0` (opens rightward instead) — safe in every current usage (`TableView.tsx`, `ArchiveView.tsx`), all of which have room to the right. Commit `c2bcfe0`.
- **XLSX export added to the Table view only, not the Kanban board (2026-07-23), not a numbered milestone, user request.** New "Export XLSX" button in `TableView.tsx`'s filter toolbar, exporting the *currently filtered+sorted* rows (not the full unfiltered set) via new `src/lib/xlsxExport.ts` (`buildApplicationsXlsx`, `triggerXlsxDownload`). **Library decision:** the obvious npm package (`xlsx`, SheetJS) has an unpatched high-severity `npm audit` advisory (prototype pollution + ReDoS) — SheetJS ships fixes via their own CDN, not npm, so the npm-published version is permanently stuck vulnerable. The vulnerable code path is in *parsing* untrusted files, which this feature never does (write-only, from trusted in-memory data), but the user chose not to accept the advisory into `package.json` regardless — went with `exceljs` (MIT, actively maintained, zero `npm audit` findings for the direct dep; one moderate transitive `uuid` buffer-bounds advisory accepted as out-of-scope edge-case risk) instead. **Performance fix applied after the fact:** the first cut imported `exceljs` eagerly at module top, which pushed the production bundle from ~944kb to ~1.9MB for every visitor even though most never export — changed to a dynamic `await import('exceljs')` inside `buildApplicationsXlsx`, restoring the main chunk to ~950kb with `exceljs`'s ~930kb split into its own lazy chunk (confirmed via `npm run build` output, before/after). Verified live: exported buffer starts with the `PK\x03\x04` zip signature every valid `.xlsx` has (checked via `javascript_tool` calling `buildApplicationsXlsx` directly and inspecting the first 4 bytes), correct size, both before and after the dynamic-import change. Columns: Company/Role/Stage/Date applied/Salary range/Location/Employment type/Work mode/Priority/Job link/Notes — mirrors `csvExport.ts`'s field choices minus Tracker/Archived-state (not applicable; Table view is already scoped to one tracker's non-archived rows). Filename `jobtracker-{tracker-slug}-{date}.xlsx`, same convention as the Insights CSV export. **Button placement**, per explicit user follow-up: moved from leftmost to the right edge of the same toolbar row (`ml-auto` on the button, filters stay left) — see the dropdown-clipping bug this triggered, above. Commits `f7e4282` (feature), `a0a70fe` (reposition).
- **Sidebar feedback box (2026-07-23), not a numbered milestone, user request.** New "Feedback" sidebar nav item (between Support-this-project and the Reminders toggle) opens `FeedbackModal.tsx`: a 1–5 star rating (required) plus an optional free-text comment, submitted via new `src/lib/feedback.ts` (`submitFeedback`) straight to a new `feedback` table (`supabase/migrations/0012_feedback.sql`, RLS insert-only — `user_id is null or auth.uid() = user_id`, so a guest can only submit with `user_id` null and a signed-in user can't forge someone else's id). **Deliberately no SELECT policy and no in-app admin view** — reviewed directly via the Supabase dashboard's Table Editor (connects as project owner, bypasses RLS), per explicit user choice over building a read-back UI. Migration `0012` has been run by the user against the live Supabase project (confirmed) and a live submission was verified end-to-end in the browser preview (4-star + comment → "Thanks for the feedback!" → row landed in the real table). Commit `68f4534`.
- **Privacy policy made directly linkable + extension data-flow disclosed (2026-07-23), not a numbered milestone.** Follow-on to the browser-extension packaging work below — Chrome Web Store needs a privacy-policy URL a reviewer can open directly, but the existing `PrivacyPolicy.tsx` view was pure client state (`view === 'privacy'` in `Board.tsx`) with no real route. Added `/privacy` as a real, bookmarkable path: `Board.tsx`'s `view` state now initializes from `window.location.pathname` and syncs back to it via `history.pushState` (plus a `popstate` listener so the back button works), and `public/_redirects` (`/* /index.html 200`) was added so Cloudflare Pages serves the SPA shell for a direct `/privacy` load instead of 404ing — confirmed in `dist/` after `npm run build`. Live-verified: direct navigation to `/privacy` deep-links straight to the policy, "Back to board" reverts the URL to `/`. Separately, the policy text was missing any mention of the browser extension's own data flow (it scrapes a job posting's page text and sends it to Anthropic via `extract-job-details`, distinct from the app's screenshot-based "Extract with AI") — added a "Browser extension" section describing that, confirmed accurate against `background.js`/`extract-job-details/index.ts` before writing it. Commit `168759d`. The Chrome Web Store submission itself is still the user's own dashboard work, not started this session (see `HANDOFF.md`).
- **Browser-extension Chrome Web Store packaging (2026-07-23), not a numbered milestone.** User asked how to publish `extension/` publicly; this covered the mechanical prep, not the actual submission (that's the user's own dashboard work, still pending — see "Open questions" in `HANDOFF.md`). Added real PNG icons (`extension/icon-16.png`/`icon-48.png`/`icon-128.png`), rasterized from the existing `public/favicon.svg` design — this closes the "Known limitation: no custom icon" gap noted in `extension/README.md` since the M8/browser-extension milestone (the README section was removed as part of this work). **How the icons were actually generated, worth remembering:** neither `rsvg-convert`/`imagemagick`/`inkscape` nor a system `cairo` lib were available, and the sandboxed browser-preview tool renders any local file outside the actual project directory as a static (non-scripting) snapshot — even a temp file placed *inside* the project directory itself still rendered inert, so a canvas-based SVG→PNG render via that tool was a dead end. Fell back to a from-scratch `Pillow` render in a throwaway `venv` (`python3 -m venv` + `pip install pillow`, no `cairosvg`/native `cairo` needed) that manually replicates the favicon's four shapes (background circle, white rounded rect, green badge circle, checkmark) scaled per target size with 4x supersampling + `Image.LANCZOS` downsample for anti-aliasing — visually confirmed via `Read` against the source SVG. New `extension/manifest.prod.json`: identical to `manifest.json` except it drops the `http://localhost:5173/*` host permission and content-script match (Chrome Web Store review flags localhost host permissions in a submitted build) — the two files must be kept in sync by hand for any future manifest field change, there's no build step generating one from the other. New `extension/package.sh` (executable) stages `background.js`/`content-bridge.js`/`popup.html`/`popup.js`/the three icon PNGs + `manifest.prod.json` renamed to `manifest.json` into a temp dir, zips it to `extension/job-tracker-extension.zip`, and cleans up the temp dir via a `trap`. Verified once by running it and inspecting `unzip -l` output (manifest at zip root, all 8 expected files, no localhost string present). Added `extension/*.zip` to `.gitignore` since it's a build artifact, not source — the zip itself is not committed. `extension/README.md` gained a "Packaging for the Chrome Web Store" section pointing at `package.sh`.
- **Header page-labels on Board/Table + sortable Table columns + tracker-reorder signed-in bug fix + extraction char-cap lowered with truncation tracking (2026-07-23), not a numbered milestone, four small pieces of follow-on work in one session.** All pushed to `origin/main`.
  - **Header labels:** `Board.tsx`'s `pageTitle` logic (around line 768) previously only set a page label for `archive`/`insights`/`privacy` views, leaving Board and Table showing bare "JobTracker" with no `/ X` suffix (Archive/Insights/Privacy already had it from earlier sessions — see the branding note below, which is now stale on this point). Added `'board' → 'Board'` and `'table' → 'Table'` branches so the header reads "JobTracker / Board" and "JobTracker / Table" too, consistent with the other views. User briefly asked to strip "JobTracker" down to just the logo in the main header (tested live, looked wrong) and separately to strip it from the *sidebar* wordmark instead by mistake — both reverted; the header text is unchanged from "JobTracker / Board" today. Commit `7028dfd`.
  - **Table sorting:** `TableView.tsx` already had click-to-sort for Company/Role/Stage/Date applied (built in M10); Location, Employment, and Work mode were static headers. Added a second `RIGHT_COLUMNS` array reusing the exact same `handleHeaderClick` toggle button as the existing `COLUMNS`, and three new `TableSortKey` variants (`location`, `employment_type`, `work_mode`) in `src/lib/tableView.ts`'s `compare()` — sorted alphabetically by *display label* (`EMPLOYMENT_TYPE_LABELS`/`WORK_MODE_LABELS`), not the raw enum value, with `?? ''` so untagged rows sort first ascending. Salary was deliberately left non-sortable — user's own column list when asking for this didn't include it, so it wasn't added. Same commit `7028dfd`.
  - **Tracker-reorder signed-in bug (real bug, found by live user report, not by inspection):** the drag-to-reorder feature shipped last session (`60f2822`) reordered and then snapped back within roughly a millisecond, but *only for signed-in users* — guest mode worked fine, which is why last session's own testing (done as a guest, since real native HTML5 drag-and-drop can't be driven by this repo's browser-preview tool) never caught it. Root cause: `useTrackers.ts`'s `refresh()` has three code paths — guest (`else` branch) and the signed-in error-fallback both `.sort(byTrackerOrder)`, but the signed-in *success* path (`list = await getAllRemoteTrackers(userId)`) did not. `reorderTrackers()` applies the new order optimistically via `setTrackers(reordered)`, writes it, then calls `refresh()` to reconcile — for a signed-in user this immediately re-fetched from Supabase in whatever raw order the query returned and overwrote the optimistic reorder, which is the visible "snap back." Fix: added `list.sort(byTrackerOrder)` right after the `getAllRemoteTrackers` call, matching the other two paths. One-line fix, `useTrackers.ts` around line 15. Commit `d825521`. **Verification method note for future sessions:** confirmed the *logic* was correct beforehand via synthetic `DragEvent` dispatch through `javascript_tool` (same technique as last session) plus a page reload to check persistence — that passed even before this fix, because guest mode (which is what a synthetic/scripted test naturally exercises without real credentials) was never broken. The bug only reproduces signed-in, which needs a human with real Supabase credentials to confirm — Claude cannot sign in itself (credential-entry is off-limits) so this fix was verified by the user, not by Claude, after being told the specific reasoning for why it should fix it.
  - **Extraction char cap 20,000 → 8,000 + truncation tracking:** user flagged the existing `MAX_TEXT_CHARS`/`MAX_EXTRACTION_TEXT_CHARS` = 20,000 (set last session when the browser-extension text-extraction path was built) as excessive token spend for a worst-case page. Real usage averages ~4,372 chars/extraction per the M8 token-cost note below, so 8,000 (~2x typical, ~2,000 tokens worst-case) was picked over 5,000/12,000 alternatives the user was offered. Lowered in all three places that must stay in sync (no build step ties them together): `supabase/functions/extract-job-details/index.ts`'s `MAX_TEXT_CHARS`, `src/lib/extensionHandoff.ts`'s `MAX_EXTRACTION_TEXT_CHARS`, `extension/background.js`'s own `MAX_TEXT_CHARS` — plus the number hardcoded in `extension/README.md`. `FUNCTION_VERSION` bumped to `extract-job-details@2026-07-23.1`. Separately, user asked what happens when a real page exceeds the cap — turned out the server's `pageText.length > MAX_TEXT_CHARS` rejection (index.ts, "Page text is too long to extract from.") is **effectively dead code reachable only by bypassing the app**, because the client (`extensionHandoff.ts`'s `parseExtensionMessage` and `background.js`'s own scrape logic) already silently truncates before the request is ever sent — so today, a user on a long page gets extraction silently run on a truncated prefix with zero error and zero visibility. User chose "keep truncating, just log it" over "reject with a visible error" (the latter would be a real UX regression for legitimately-long-but-previously-fine postings). Implemented: `background.js` now measures `trimmedText.length` *before* its own slice and sends it as `originalTextLength` in the postMessage payload; `extensionHandoff.ts`'s `ExtensionHandoffPayload` gained an optional `originalTextLength?: number` threaded through `parseExtensionMessage`; `Board.tsx` → `remoteStore.ts`'s `extractJobDetailsFromText(text, originalTextLength?)` → the Edge Function, which computes `originalTextChars = mode === 'text' ? (originalTextLength ?? pageText!.length) : null` and writes it as the new nullable `original_text_chars` column (`supabase/migrations/0011_extraction_original_text_chars.sql`) alongside `input_tokens`/`output_tokens` on the `extraction_events` row. A truncated extraction is queryable as `select count(*) from extraction_events where original_text_chars > 8000`. Commit `08b793f`. **Three manual steps still owed by the user, not yet confirmed done:** (1) run migration `0011`, (2) redeploy `extract-job-details` via the Supabase dashboard, (3) reload the unpacked extension in `chrome://extensions` so it picks up the new `background.js`. Until all three happen, `original_text_chars` will not actually be populated even though the code is live.
  - **Test suite note:** `npx vitest run` shows 46 pre-existing failures unrelated to this session (confirmed identical failure count on `main` via `git stash` before these changes — looks like environment/timer flakiness, e.g. `useStaleReminders.test.ts` "Cannot read properties of undefined (reading 'clear')"). Not investigated further this session since none are in files touched here; worth a dedicated look in some future session if it keeps showing up, since 46/~130 failing is a lot of test-suite noise to have accumulated. New tests added this session (2, in `extensionHandoff.test.ts` for `originalTextLength` pass-through) do pass.
- **Tracker drag-to-reorder (2026-07-23), not a numbered milestone.** Trackers previously only ordered by `created_at` with no way to rearrange tabs. `TrackerTabs.tsx` now supports dragging a tab to a new position. **Two implementation attempts, the second replacing the first:** v1 live-reshuffled the rendered tab list on every `dragover` (`reorder(dragPreview, draggingId, index)`, feeding its own output back in as the next input) — worked in principle but the user reported real jitter/glitch on small mouse movements, plus the browser's native drag-ghost image visibly trailed the cursor on the y-axis, which the user also didn't want (asked for x-axis-only movement). Rebuilt as v2: tab layout stays static during the whole drag (no live reshuffle, so no feedback loop to jitter); a thin indicator bar (`bg-ink-400`, `w-0.5`) shows the computed drop index instead; drop index is computed purely from cursor `clientX` vs. each tab's horizontal-midpoint (via a `tabRefs` Map of id→element), so vertical mouse movement has zero effect; the native drag-ghost is suppressed via `e.dataTransfer.setDragImage()` pointed at a 1x1 transparent GIF. Reorder is applied once, on drop, via array splice math (`insertAt = dropIndex > fromIndex ? dropIndex - 1 : dropIndex`). **Persistence:** new optional `Tracker.sort_order?: number` field (optional so existing rows/tests didn't need updating — `byTrackerOrder` in `src/lib/sort.ts` falls back to `created_at` when unset), `supabase/migrations/0010_tracker_sort_order.sql` (nullable `integer` column, no backfill needed), `useTrackers.ts` gained `reorderTrackers(orderedIds)` writing a concrete `sort_order` to every tracker in one pass (local IndexedDB + Supabase if signed in), and `createTracker` now stamps new trackers with `max(existing sort_order) + 1`. **Testing gotcha:** this repo's browser-preview tool can't drive real native HTML5 drag-and-drop (confirmed separately for dnd-kit/card-dragging in M11 too) — verified correctness instead via `javascript_tool` dispatching synthetic `DragEvent`s with real inter-event delays (`await sleep(...)` between `dragstart`/`dragover`/`drop`, since React needs a tick to re-render `draggingId` state between synthetic events fired back-to-back in the same call stack — a naive single-shot dispatch is a false negative, not a real bug). Migration run manually against the live Supabase project by the user. Commit `60f2822`, pushed (`9b8bc77..60f2822`). `graphify-out/` was also added to `.gitignore` this session (commit `5279252`) — see the Graphify note below.
- **Graphify installed (2026-07-23), not a numbered milestone, tooling not app code.** Installed `graphifyy` (PyPI, via `uv tool install` — `uv` itself installed via `brew install uv` since the system Python was 3.9.6 and graphify needs 3.10+) and registered its `/graphify` skill globally (`~/.claude/CLAUDE.md` + `~/.claude/skills/graphify/`, not project-scoped — user's explicit choice after being shown a caution: the repo has an unusual 94k-star/9k-fork count for a 4-month-old niche dev-tool repo, flagged but not treated as disqualifying). Built a **code-only** graph for this repo (`--code-only`, no LLM key configured, no data left the machine): `graphify-out/graph.json`, 520 nodes / 1180 edges / 23 communities, built from commit `60f28226`. Community labels are generic "Community N" placeholders since labeling needs an LLM key (`GEMINI_API_KEY`/`GOOGLE_API_KEY`) that isn't set — expected, not a bug. `graphify-out/` is gitignored (regenerable local tool output, not project source). **User's standing instruction, saved to Claude's cross-session memory (not this file — it's a Claude Code behavioral preference, not project truth):** re-run `graphify update .` proactively (no need to be asked) whenever `git rev-parse HEAD` has moved past the graph's "Built from commit" line and a graph-shaped question is about to be answered, rather than waiting for an explicit request each time.
- **Board multi-select + bulk actions — done, pushed, live-verified (2026-07-23), not a numbered milestone.** Cmd/Ctrl+click toggles cards into a selection (visual: amber-ish ring via a new `selected` prop threaded through `Column.tsx` → `Card.tsx` → `CardVisual.tsx`); a plain click while any card is selected clears the selection instead of opening detail (confirmed with the user before building — the "first click just exits multi-select" behavior was an explicit design decision, not a default). Two entry points drive the same bulk-action list: right-click on a *selected* card, and a new fixed-bottom `SelectionToolbar.tsx` (shown whenever the selection is non-empty) with, left to right, a star icon (bulk most-wanted toggle — single click, no menu item; tri-state logic: marks all unless every selected card is already priority, in which case it unmarks all), an "Actions ▾" button, the "N selected" count, and "Deselect" — that exact order was a explicit user-requested reorder from the initial count/Deselect/star/Actions layout. The bulk menu is grouped into exactly 3 top-level choices — "Move to stage ▸", "Archive ▸", "Delete" — each stage/reason list reachable only by drilling in (with a "← Back"), per explicit user ask ("if a user only wants to move or archive them they don't need to see the other type of action"); most-wanted was deliberately pulled OUT of the menu entirely once the star icon existed. **New reusable capability added to `ContextMenu.tsx` to support this:** `ContextMenuItem` gained an optional `items?: ContextMenuItem[]` for click-to-drill (not hover-flyout) submenus, via an internal stack captured ONCE at mount (`useState(() => [items])`, deliberately not resynced to the `items` prop on later re-renders, since callers rebuild that array fresh every render — resyncing would reset the drill-down back to top level after every click). Own test file `ContextMenu.test.tsx` covers the stack/back/leaf-execution behavior directly, separate from Board's integration tests. **Real bug found via live testing, not by inspection:** `SelectionToolbar`'s outer bar div uses `-translate-x-1/2` (a CSS transform) to center itself, and any `position: fixed` descendant of a transformed ancestor positions relative to THAT ancestor instead of the viewport — so `ContextMenu` rendered as a child of the bar opened off-screen (measured at DOM level: anchor (603,769) in a 1156×837 viewport rendered at (1056,1204)). Fixed by rendering `ContextMenu` as a JSX sibling of the transformed div, not a child — worth remembering for any future fixed-position popup nested inside a `translate-*`/`scale-*`/other-transformed Tailwind element. **Hook change:** `useApplications.ts`'s `togglePriority(id)` gained an optional second param, `togglePriority(id, value?: boolean)` — when passed, sets that exact value (no-op if already equal) instead of flipping; needed because the bulk star toggle must set an unconditional true/false across a mixed-state selection, not flip each card independently. Bulk archive reuses the existing undo toast/timer, generalized from `undoState: {id, company}` to `{ids: string[], label: string}` so Cmd/Ctrl+Z and the toast's own Undo button restore an entire archived batch. `deleteApplicationTarget` (single `Application`) similarly generalized to `deleteApplicationTargets: Application[] | null`, and `DeleteApplicationModal` now takes `applications: Application[]` with pluralized copy when length > 1 — used for both the single-card delete path and bulk delete, one code path. Commit `594b6fc`, pushed to `origin/main` (`acfa6a8..594b6fc`). 127 tests passing (up from 115 pre-session), `tsc`/`oxlint` clean, verified live in the browser preview (ctrl-click select, right-click bulk archive + undo, toolbar bulk move, drill-down menu, star icon, Escape-to-clear, plain-click-clears).
- **Sidebar nav rename: "Job Tracker" → "Board" (2026-07-23), not a numbered milestone.** User flagged that the sidebar's kanban nav item being labeled "Job Tracker" duplicated the "JobTracker" logo/wordmark directly above it, and made "Table" look like an unrelated section rather than a sibling view of the same data. One-line fix in `Sidebar.tsx` (`label="Job Tracker"` → `label="Board"`); no other code referenced the old string. Commit `acfa6a8`, pushed (`9dd423d..acfa6a8`).
- **Insights CSV export (2026-07-23), not a numbered milestone.** New "Export CSV" button on the Insights page (`InsightsView.tsx`), next to the existing tracker-scope dropdown, exports the currently-scoped raw applications (not chart aggregates — user chose "raw applications table" over per-chart exports when asked) as CSV via new `src/lib/csvExport.ts` (`escapeCsvField`, `buildApplicationsCsv`, `triggerCsvDownload`). Columns mirror `TableView.tsx`'s existing label choices (`STAGE_LABELS`/`EMPLOYMENT_TYPE_LABELS`/`WORK_MODE_LABELS`/`ARCHIVE_REASON_LABELS`), plus tracker name (resolved by `tracker_id` lookup, since "All trackers" scope spans more than one), archived-at, job link, notes. Filename `jobtracker-{scope-slug}-{today}.csv`. Pure client-side, zero Anthropic/token involvement — confirmed to the user before building since M8's screenshot-extraction feature is the only Anthropic touchpoint in this repo and this isn't it. `csvExport.test.ts` covers escaping (comma/quote/newline), empty-vs-populated fields, multi-tracker name resolution, empty input. Commit `9dd423d`, pushed (`806d92b..9dd423d`).
- **M12 — Insights page + a 5-stage correctness audit of its own math — done, all 5 stages shipped (`cfbbc45`, `4eaada2`, `ae2b170`, `86288e3`) and live-verified on `jobtracker.fazare.dev`.** New "Insights" sidebar nav item with a cumulative funnel, KPI tiles, and 7 charts on `recharts`. Full build + audit narrative moved to [PLAN-ARCHIVE.md](PLAN-ARCHIVE.md) ("M12 — Insights page + a 5-stage correctness audit of its own math"). 106 tests passing (up from 75 pre-M12), `tsc`/`oxlint` clean.
- **UI reskin to dark-green palette — done, pushed, live-deploying.** Follow-on from the branding session below. Added a custom `ink-*` Tailwind v4 color scale via `@theme` in `src/index.css` (10 steps, same lightness curve as Tailwind's default `slate` so contrast ratios are unchanged, hue shifted to 152deg toward the logo's greens `#1c3a27`/`#1fa04e`). Presented the user 3 candidate chroma levels (Subtle/Medium/Bold) as an artifact with real sidebar+board mockups per swatch, generated via the same browser-canvas OKLCH→hex probe trick used for the logo; user picked **Bold** (`--color-ink-50: #f2fdf5` … `--color-ink-900: #031e0c`, full 10-step list in `src/index.css`). Mechanically renamed `slate-*` → `ink-*` across all 25 component files (205 usages) via `sed`. **Gotcha hit and fixed:** naive `sed 's/slate-/ink-/g'` also matches the `slate-` substring inside `translate-`, corrupting `translate-x-1/2` → `tranink-x-1/2` in `ErrorToast.tsx` and `UndoToast.tsx`; caught via a post-replace `grep -rohE "\b[a-zA-Z]*ink-[a-zA-Z0-9]*"` sweep and fixed with a second targeted `sed`. **Worth remembering for any future bulk Tailwind class rename: always grep for the new token afterward looking for accidental word-boundary collisions, not just confirm the old token is gone.** Commit `b60ed7a`. Follow-up in the same session: user noted the `Sidebar.tsx` panel was still `bg-white`, standing out against the now-green board — changed to `bg-ink-200`/`border-ink-300` (one step darker than the board's `bg-ink-50`, with the active-nav-item pill at `ink-100` staying lighter than the panel so it still reads as "selected"). Commit `426dd20`. Both commits pushed to `origin/main` (`b9ce75f..426dd20`); Cloudflare auto-deploy from `main` should pick it up, **not yet re-verified live on `jobtracker.fazare.dev` after this push** — worth a quick visual check next session before assuming it's live. `tsc`/oxlint/75 tests all clean after both commits; visually verified locally (`npm run dev`) across Board, Table, Archive, and the Add Application modal. App has no dark-mode variants (light-only by original design) — `prefers-color-scheme: dark` was checked and confirmed to change nothing, which is expected/unrelated to this change, not a bug.
- **Branding: JobTracker logo + favicon added (2026-07-22), not a numbered milestone.** User supplied a Claude-Design export (zip: "JobTracker Logo v2.dc.html", green folder+checkmark mark on a dark forest circle) via `/Users/burak2/Downloads/JobTracker logo design.zip`. Built `src/components/Logo.tsx` (`LogoMark`, hex colors converted from the design's OKLCH values via a canvas-fillStyle probe in a live browser tab: dark `#1c3a27`, accent `#1fa04e`, light `#c9d0ca`), replaced `public/favicon.svg` with the 32px favicon variant, added `apple-touch-icon` in `index.html`, and wired the mark + "JobTracker" wordmark into both `Sidebar.tsx` (collapsed/expanded states) and `Board.tsx`'s header (replacing the old plain "Job Application Tracker" `<h1>`; header read "JobTracker" alone on board/table at the time, or "JobTracker / Archive" / "JobTracker / Privacy policy" on those views — **note: as of the 2026-07-23 session above, Board and Table also show a "/ X" suffix, so this is no longer accurate for those two views**). Verified live on production (`jobtracker.fazare.dev`, Cloudflare auto-deploy from `main`) after push — user confirmed the favicon eventually showed correctly once their browser's separately-cached favicon was cleared (hard refresh / new tab), which was the only snag, not a deploy issue. Commit: `9e01005`.
- **M11 — Mobile-first polish — done, typechecked/linted/tested, verified live (iOS Simulator + browser preview).** Scope was narrowed to the three concrete gaps identified by reading the code, not a redesign (brief §8: "PC-first; keep mobile functional but basic"). (1) `Sidebar.tsx` was hover-only expand (`w-14 hover:w-56`), unreachable on touch devices with no hover — added a `MenuIcon` tap-to-toggle button driving an explicit `expanded` state, combined with the existing `:hover` behavior via `!opacity-100` overrides so desktop mouse users see no change; verified via accessibility-tree checks in a 375×812 mobile viewport that tap correctly expands/collapses independent of hover. (2) Board columns (`Column.tsx`) already had `w-72 shrink-0` and scroll cleanly via the board's `overflow-x-auto` — checked live at mobile width, no change needed. (3) **Real bug found and fixed**: cards (`Card.tsx`, wired to `useDraggable`) had no `touch-action` CSS, so on a touchscreen the browser's native scroll gesture won the race against dnd-kit's `PointerSensor` before it could activate — dragging a card scrolled the board horizontally instead of moving it (dnd-kit's own docs flag this as the standard PointerSensor+touch gotcha). Fixed with `touch-none` on the draggable card div. Confirmed both broken (pre-fix) and fixed (post-fix) live in the iOS Simulator (iPhone 17, Safari) using a real multi-point touch-drag gesture: TestCo/Engineer moved from "Applied" to "Eyes on" correctly after the fix. The browser-preview tool's own drag simulation (single-shot mouse move) can't exercise dnd-kit at all, mouse or touch — a tool limitation unrelated to this fix, confirmed separately.
- **M10 — Alternate view: sortable/filterable Table view — done, typechecked/linted/tested, live-verified.** New "Table" nav item in `Sidebar.tsx` (between Board and Archived), scoped to the active tracker's non-archived applications (mirrors Board's scope, not Archive's cross-tracker one). `src/components/TableView.tsx` renders Company/Role/Stage/Date applied/Salary/Location/Employment/Work mode columns plus a priority star; clicking a row opens the existing `CardDetail` modal (no new detail UI). Click-to-sort column headers for company/role/stage/date (stage sorts by pipeline order via `STAGE_ORDER`, not alphabetically) — pure logic extracted to `src/lib/tableView.ts` (`sortApplicationsForTable`) with its own tests. Filtering reuses the existing `MultiSelectFilter` component (same pattern as `ArchiveView`) for Stage/Employment/Work mode, with the same "untagged rows are unaffected by that filter" rule. No drag-and-drop in this view (brief allows non-board views to skip it); stage change instead via a per-row `<select>` wired to the same `moveApplicationStage` used by the board's drag handler, so `stage_history` still gets a row. Archiving is not available from the table (still board/detail-modal only) — flagged as a possible follow-on, not built. Verified live in the browser preview: nav item appears, table renders real guest data, column-header sort (company asc/desc) and the Stage filter dropdown both work.
- **`AUDIT.md` — fully closed 2026-07-22, by explicit user decision not to be revisited.** Every High/Medium/Low finding, every missing-piece and needs-verification item, is fixed and live-confirmed — including a final `git log --grep` completeness check that resolved two checklist items that *looked* outstanding but were actually stale checkboxes (see "Codebase audit + remediation (AUDIT.md)" in [PLAN-ARCHIVE.md](PLAN-ARCHIVE.md) for the full record, including the D6 Anthropic-balance decision).
- **M9 — Follow-up reminders (push only, no email) — done, live-tested by user in their own browser.** Auto-trigger on time-since-stage-change (14-day threshold, `src/lib/stale.ts`), delivery via the browser `Notification` API while the tab is open/backgrounded (no service worker/VAPID/closed-browser push), on/off toggle (bell icon) in `Sidebar.tsx` requesting OS permission on click, `src/hooks/useStaleReminders.ts` polling every 5 min + on mount with dedup/batching via `src/lib/reminders.ts`. Card badge (front-of-card only, renamed "Stale" → "OUTDATED" per user request) has a per-card dismiss ✕ that suppresses both the badge and the notification for that app until it's genuinely touched again (`dismissStale`/`isDismissedStale` in `src/lib/reminders.ts`). User ran it live via their own `npm run dev` with the threshold temporarily set to 0 for fast iteration, asked for the text rename and the dismiss control mid-session, then had it reverted to 14. User explicitly confirmed "yes it worked" after testing. 70 tests passing, `tsc`/`oxlint` clean.
- Prior: All 4 user-proposed features are shipped and live-verified: (4) rename trackers, (3) right-click context menu, (1) most-wanted priority, (2) the browser extension — user loaded it unpacked in a fresh Chrome install and confirmed a real job posting's page pre-filled the add-application form correctly end-to-end.
- **Last completed before M8:** M7 — compulsory name at sign-up, a single Account panel (name/email/password/export/delete/sign-out), and a unified `account-action` Edge Function covering both delete and change-password. Verified live end-to-end by the user on real accounts, including catching and fixing a real bug where changing your password silently revoked your own current session (see "M7 — Account panel + names" below for the full story: a first attempt reverted entirely, rebuilt with each Edge Function action curl-tested standalone, then a second bug found live during QA itself and fixed by removing the risky feature rather than continuing to patch it).
- **App runs?** yes — both locally (`npm run dev`) and live in production
- **Resend domain verified:** user bought `fazare.dev` on Cloudflare, verified it in Resend, and updated Supabase's custom SMTP to send from it — the sandbox "only sends to the account owner's own email" restriction is gone. Confirmed working live (bogus-login test hit Supabase's real Auth API from the deployed site).
- **Session tooling:** `.claude/commands/continue.md` and `.claude/commands/handoff.md` now exist (`/continue` to re-prime a fresh session, `/handoff` to persist state before a `/clear`). Both were generic templates originally written for a `CLAUDE.md`/`architecture.md`/`DECISIONS.md`/`IMPLEMENTATION-PLAN.md` doc structure that doesn't exist in this repo — edited to reference `PLAN.md` and `HANDOFF.md` instead, this project's actual persistent docs. If a future session considers adding those other doc files for real, revisit whether splitting `PLAN.md` up still makes sense at that size, rather than assuming the commands' original structure was right.

---

## Milestones


> **Completed milestones (M1–M7) and all shipped post-MVP work have moved to [PLAN-ARCHIVE.md](PLAN-ARCHIVE.md)** to keep this file light on tokens. One-line index:
>
> - M1 Foundation · M2 Board + manual entry · M3 Movement + detail · M4 Archive + undo · M5 Auth + guest→account migration · M6 GDPR + polish · M7 Account panel + names — all ✅ done
> - Post-MVP done: Multiple named trackers · Archive grouping & sort · Hosting (live at `jobtracker.fazare.dev`) · post-M7 sidebar polish · Permanently delete an application · Four user-proposed features (rename trackers, right-click menu, "most wanted" priority flag, browser extension) · Codebase audit + remediation (`AUDIT.md`, closed — incl. the D6 Anthropic-balance decision) · M12 Insights page + 5-stage correctness audit (closed 2026-07-23) · Insights CSV export · Sidebar "Job Tracker"→"Board" rename · Board multi-select + bulk actions · Tracker drag-to-reorder · Browser-extension Chrome Web Store packaging · Directly-linkable privacy policy + extension data-flow disclosure · Sidebar feedback box · Table-view XLSX export · Multi-select drag-to-move fix (all closed 2026-07-23) · **M13 Interview scheduling + calendar export (closed 2026-07-24)**
>
> Full checklists, bugs, and QA notes for those live in the archive. Recent/active milestones stay below.


### M8 — AI job extraction from a screenshot  *(user request)* — ✅ done, live, and verified end-to-end

**Flow:** on the add-application form, a signed-in user clicks "Extract from screenshot," picks an image, and Claude Haiku 4.5 pre-fills company/role/salary/location/link/employment-type/work-mode for the user to review and save. Free but spend-capped (20 extractions/user/month, 5,000/month global ceiling, calendar-month reset) so usage can't produce a surprise bill.

- [x] `supabase/functions/extract-job-details/index.ts` — signed-in-only Edge Function; checks the per-user and global monthly quota BEFORE calling Anthropic (the wallet protection); calls Haiku 4.5 with the image + `output_config.format` structured-output JSON schema; records one `extraction_events` row per successful call. Curl-verified standalone (real extraction, invalid input, simulated over-quota) before any UI touched it, same discipline as M7.
- [x] `supabase/migrations/0004_extractions.sql` — `extraction_events(user_id, created_at)`, RLS scoped to `select` own rows only.
- [x] Token-spend caps added after real usage data (~1,093 in / 82 out ≈ $0.0015/extraction, cheaper than the original estimate): reject images over 5MB decoded before any Anthropic call; `max_tokens` 1024 → 500.
- [x] `extractJobDetails()` in `src/lib/remoteStore.ts`, reusing a shared `invokeEdgeFunction` helper (refactored out of the existing `callAccountAction` for the same error.context-extraction pattern).
- [x] "Extract from screenshot" button in `ApplicationForm.tsx` (add-mode + signed-in only — hidden entirely for guests and when editing), with client-side type/size validation mirroring the server's 5MB cap, a loading state, and auto-fill on success.
- [x] User-run live QA on the core extraction flow: passed (button appears when signed in, extracts correctly, re-usable, rejects non-images client-side).

#### Follow-ups from that QA round — all built and verified live

- [x] **Notes no longer auto-fills from extraction** — removed from the Edge Function's schema entirely (the user wants Notes reserved for their own free-form ideas, not AI output) and from the client auto-fill.
- [x] **Employment type / work mode** — new nullable `employment_type` (`full_time`/`part_time`/`freelance`, later joined by `internship` via `0006_internship_employment_type.sql`) and `work_mode` (`on_site`/`remote`/`hybrid`) columns (`0005_employment_work_mode.sql`, same enum-naming pattern as `archive_reason`), form dropdowns in `ApplicationForm.tsx`, display in `CardDetail.tsx`, and both are now part of the AI extraction schema too. Labels centralized in `src/lib/employment.ts` (shared between the form and card detail, same pattern as `src/lib/archive.ts`). Adding a new enum value later is a Postgres `alter type ... add value` migration, not a column change — worth remembering as the pattern for any future employment_type/work_mode/archive_reason/application_stage addition.
- [x] **Employment type / work mode filter UI in the Archive view** *(follow-on, user request)* — extracted a generic `MultiSelectFilter<T>` component (`src/components/MultiSelectFilter.tsx`) from what used to be the Archive view's one-off `ReasonFilter`, then used it for three filters: Reasons (existing, now using the generic component), Employment, Work mode. Scope deliberately limited to the Archive view, matching the existing "Reasons (n)" precedent — the main board has no filter concept at all and adding one there was explicitly ruled out for this pass. Applications with no value for a given field are unaffected by that field's filter (shown regardless), rather than requiring an explicit "not specified" option. Accessibility pass (`web-design-guidelines` skill) added `aria-expanded`/`aria-haspopup` and Escape-to-close-with-focus-return to the shared component — gaps that existed in the original `ReasonFilter` too, fixed once here since three instances were about to ship instead of one. Verified live: filtering narrows correctly, panel toggle/Escape/focus-return all work, `aria-expanded` reflects state.
- [x] **Separate "Log in" sidebar button for guests** — reverses the M7-era decision that login should only be reachable via the toggle inside the Sign-up modal (see the post-M7 sidebar polish note above). Used an icon (`LoginIcon`) that was already defined in `icons.tsx` but unused.

#### Bug found and fixed during the QA round: structured-output schema rejected by Anthropic

First redeploy of the employment_type/work_mode extraction returned a generic "Extraction failed" (the function's deliberately-generic client-facing message for any non-2xx from Anthropic). Root cause: the schema expressed a nullable enum as `{ type: ['string', 'null'], enum: ['full_time', ..., null] }` — valid JSON Schema, but not a construct Anthropic's structured-output validator accepted, unlike the plain `type: ['string', 'null']` (no `enum`) used successfully by the original six fields. Fixed by switching to the documented-safe `anyOf` pattern: `{ anyOf: [{ type: 'string', enum: [...] }, { type: 'null' }] }`. Confirmed working after redeploy — worth remembering as a general pattern if a future field ever needs a nullable enum in a structured-output schema.

#### Manual steps + QA checklist for M8 — all done

1. ~~Run `supabase/migrations/0005_employment_work_mode.sql`~~ — done.
2. ~~Redeploy `extract-job-details`~~ — done, twice (once for the initial schema change, once for the `anyOf` fix above).
3. ~~Live QA of all three follow-ups~~ — done, all confirmed: dropdowns persist through a real Supabase round-trip (verified via the Table Editor too), "Not specified" round-trips as `null`, a fresh extraction leaves Notes empty and correctly fills employment type/work mode when the screenshot states them, and the new "Log in" sidebar button opens the modal directly in log-in mode.
4. Committed; PLAN.md updated.

---

## Out of scope for MVP (do not build; don't design out)

Link auto-parsing · email reminders (push reminders shipped as M9) · alternate views (table/list, sorting/filtering) · mobile-first polish · full multi-level undo · real donations integration. See brief §8.

**Import/restore (AUDIT.md C2) — ruled out, not deferred.** Flagged in the original audit as "half a safety net" (export exists, nothing reads it back in), but explicitly rejected: the per-account extraction quota (`PER_USER_MONTHLY_LIMIT = 20`, see `src/lib/extraction.ts` and `supabase/functions/extract-job-details/index.ts`) is scoped to `user_id`, not to the underlying person — an import feature would let someone export their board, create a fresh account, import it back, and get another 20 free extractions, repeatably. Don't build this later without also solving that quota-reset vector (e.g. tying limits to something account-creation can't reset), not as a simple follow-up.

---

## Postponed / deferred (not forgotten, just not now)

Things explicitly pushed to later rather than built now or ruled out. Pick any of these up whenever — none are blocking.

~~M13 — Interview scheduling + calendar export~~ — **done, 2026-07-23/24**, moved to [PLAN-ARCHIVE.md](PLAN-ARCHIVE.md). One open thread from it, not yet resolved: a live guest→signup migration test found the automatic post-signup migration failing once then succeeding on a bare retry — root cause unconfirmed (leading theory: a session-hydration race right after email confirmation). See the archive entry for full detail. A leftover Supabase test account (`jaliba2323@barumart.com`) from that test is still in the live project's `auth.users` table, safe to delete via the dashboard whenever convenient.

### Candidate next milestones (user flagged 2026-07-22)

Follow-up reminders picked and built as M9. Alternate views built as M10. Mobile-first polish built as M11. None remain from this list — next milestone, if any, needs a fresh user ask.

- **Unexplained data loss in the `applications` table** — see "Decisions & notes". Investigated and unresolved; test data only, so the user chose to move on.
~~M12 Insights correctness audit (Stages 1-5)~~ — **fully closed**, see "Current status" above.

~~`AUDIT.md`~~ — **fully closed, 2026-07-22** (moved to [PLAN-ARCHIVE.md](PLAN-ARCHIVE.md), see "Current status" above) — not deferred, not to be revisited.
~~D6 — Anthropic account balance / auto-reload decision~~ — **decided, 2026-07-22**: leave as-is; see the "D6 decision" note in PLAN-ARCHIVE.md.
~~Employment type / work mode filter UI~~ — **done**, see "M8 — AI job extraction from a screenshot" below.
~~Hosting~~ — **done**, see "Post-MVP — Hosting" below.
~~Deletion-confirmation email~~ — **done**, folded into M7's unified `account-action` function — see "M7 — Account panel + names" below.
~~Old Supabase Auth accounts from testing~~ — **done**, user deleted both leftover accounts (the real secondary email and an old test signup) directly from the Supabase dashboard, then deleted and recreated the primary account too as part of M7's live QA — the only account now is the fresh one with a name set.
~~Show which tracker an archived application belongs to, inside `CardDetail.tsx`~~ — **done**, see "Post-MVP — Archive grouping & sort" below.
~~Real Ko-fi/donation flow verification~~ — **done**, user clicked through and confirmed it leads to the correct page.

---

## Decisions & notes

The full decisions-and-gotchas log — stack choices, Supabase project/key facts, data-layer architecture, the guest-migration-trigger redesign, testing tooling, Edge Function deploy-sync rules, and every per-milestone bug post-mortem — lives in **[PLAN-ARCHIVE.md](PLAN-ARCHIVE.md)**. It is historical reference: read the specific entry there when a task touches that area, rather than loading all of it every session.

Record new decisions from the *current* active milestone here first; fold them into the archive at the next `/handoff` once that milestone is done.
